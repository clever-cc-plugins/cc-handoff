name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    uses: clever-cc-plugins/.github/.github/workflows/plugin-release.yml@v1
    permissions:
      contents: write
